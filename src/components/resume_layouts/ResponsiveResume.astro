---
import classNames from "classnames";

import ResumeSection from "src/components/ResumeSection/ResumeSection.astro";
import ResumeSectionOld from "src/components/ResumeSectionOld/ResumeSectionOld.astro";

import {
  brief,
  certs,
  languages,
  libraries,
  tools,
  platforms,
  skills,
  education,
  curriculum_vitae,
} from "src/resume_content.js";

import responsive_styles from "./ResponsiveResume.module.scss";
import common_styles from "./resume-common.module.scss";
---

<div
  class:list={classNames(
    common_styles.resume,
    responsive_styles["responsive-resume"]
  )}
>
  <ResumeSection
    className={classNames(responsive_styles["primary-section"])}
    resume_section_id={"main_header"}
  />
  <ResumeSectionOld enterFrom="left" section={brief} />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="right"
    section={certs}
  />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="left"
    section={languages}
  />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="right"
    section={libraries}
  />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="left"
    section={tools}
  />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="right"
    section={platforms}
  />
  <ResumeSectionOld
    className={classNames(common_styles["horizontal-list"])}
    enterFrom="left"
    section={skills}
  />
  <ResumeSectionOld enterFrom="right" section={education} />
  <ResumeSectionOld enterFrom="left" section={curriculum_vitae} />
</div>
