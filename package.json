{
  "name": "resume",
  "version": "1.0.0",
  "homepage": "",
  "dependencies": {
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-markdown": "^4.1.0",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate_pdf": "node scripts/generate_pdf.js",
    "test_pdf_page_count": "node scripts/test_pdf_page_count.js",
    "predeploy": "npm run build && npm run generate_pdf",
    "deploy": "npm run test_pdf_page_count && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "get-port": "^5.0.0",
    "gh-pages": "^2.1.1",
    "puppeteer": "^2.0.0",
    "serve": "^11.2.0"
  }
}
